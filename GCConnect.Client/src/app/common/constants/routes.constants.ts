export const ROUTES = {
    root: '',
    loginCallback: 'login/callback',
    auth: 'auth',
    profile: 'profile',
    dashboard: 'dashboard',
} as const;

export const ROUTES_PATHS = {
    root: '/',
    loginCallback: '/login/callback',
    auth: '/auth',
    profile: '/profile',
    profileDetails: '/profile/details',
    dashboard: '/dashboard',
} as const